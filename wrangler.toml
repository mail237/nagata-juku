# Cloudflare Pages: static HTML export (`next build` → `out/`) + `/functions` API.
# `name` must match your Pages project name in the Cloudflare dashboard.
name = "nagata-juku"
compatibility_date = "2024-12-01"
pages_build_output_dir = "./out"

# 平文の環境変数は wrangler に書く（ダッシュボードは Secret のみ追加可）。
# Resend で許可した送信元アドレスに合わせて変更してください。
[vars]
CONTACT_EMAIL_FROM = "永田塾 <mail@nagata-juku.net>"
